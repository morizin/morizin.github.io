/* Deployment config — not content. Edit after deploying worker/ (see worker/README.md).
   Leave `api` empty and the site falls back to on-device keyword search. */
window.MORIZIN_CONFIG = {
  api: ''   // e.g. 'https://morizin-ai.<your-subdomain>.workers.dev'
};
